# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Local secrets. The two committed files are deliberate: .env.example
# documents the variables, and .env.production carries the public API URL
# Vercel needs at build time (it holds no secrets).
.env*
!.env.example
!.env.production
